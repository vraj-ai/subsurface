[package]
name = "subsurface-engine"
version = "0.1.0"
edition = "2021"

[features]
default = []
test-fixtures = []

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
chrono = { version = "0.4", features = ["serde"] }
tempfile = { version = "3.14", optional = true }

[dev-dependencies]
tempfile = "3.14"
