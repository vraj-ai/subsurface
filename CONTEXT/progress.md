# Progress

- Current milestone: v1.1 M2 — deterministic provider contracts and connection lifecycle.
- Last verified milestone: v1.1 M1 — canonical Project vocabulary and commands,
  lossless legacy persistence migration, and durable activity lifecycle
  (`9c540c27`).
- Verification: `cargo test --workspace`, the M1 migration and activity suites,
  `git diff --check`, and the release build passed. Independent milestone review:
  PASS.
